import { DetectorPage } from "@/components/detector/DetectorPage";

export const metadata = {
  title: "Detector en Vivo · SafeCampus AI"
};

export default function DetectorRoute() {
  return <DetectorPage />;
}
